[package]
name = "comms-project"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
[target.'cfg(any(target_arch = "arm", target_arch = "aarch64"))'.dependencies]
serialport = "4.8.1"
